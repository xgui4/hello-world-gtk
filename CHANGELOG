7 February : 
    - reorganised the pkg folder  
    - re-add flatpak support 
        (but the app still need to implement some portals and the manifest might need to be changed)
    - a local make pkg file that work
    - added WIP github action workflow
    - Update the vscode actions 
    - update the clangd setting
    - updated the readme 
    - temporaly disabled the github action

6 February : fix the PKGBUILD file and PKGBUILD file + fix a security flaw in the cli command 

5 February : Removed useless header, added vscode support for clang and clangd and codelldb debugging support with meson

4 February : Begin the switch to Meson Build, and deleting old obseled file that used makefile and new branch -> switch to meson  
