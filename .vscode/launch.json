{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Executable",
            "program": "${workspaceFolder}/build/hello-world-gtk",
            "args": [],
            "env" : {"CC": "clang"},
            "cwd": "${workspaceFolder}/build",
            "envFile": "${workspaceFolder}/builddir/meson-vscode-env.env",
            "preLaunchTask": "Configure & Build"
        }
    ]
}
