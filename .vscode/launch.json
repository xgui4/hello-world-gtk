{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Executable",
            "program": "${workspaceFolder}/builddir/hello-world-gtk",
            "args": [],
            "env" : {"CC": "clang"},
            "cwd": "${workspaceFolder}/builddir",
            "envFile": "${workspaceFolder}/builddir/meson-vscode-env.env",
            "preLaunchTask": "Configure & Build"
        }
    ]
}
