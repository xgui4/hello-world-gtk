{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make -f makefile.debug build",
            "type": "shell",
            "command": "make -f makefile.debug clean && make -f makefile.debug build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
